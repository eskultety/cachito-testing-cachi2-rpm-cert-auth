# dnf install qemu-user-static
# systemctl start systemd-binfmt
# install https://github.com/konflux-ci/rpm-lockfile-prototype
# rpm-lockfile-prototype --debug rpms.in.yaml

contentOrigin:
  repofiles:
    - test.repo
packages:
    - bash

arches:
  - x86_64
  #- aarch64
  #- ppc64le
  #- s390x
context:
    bare: true
